Search the web now and answer these 12 questions. I need a source URL for each answer.

If you cannot find a source for one, write "could not verify" and move on. Do not guess.
Answer as many as you can. Number your answers to match.

1. What context window do these coding tools use, as of September 2026: Claude Code, OpenAI Codex, GitHub Copilot agent mode, Google Antigravity, Cursor, Devin Desktop (formerly Windsurf)? Which of them do not publish the number?

2. When a coding agent runs a task unattended, what does it do without asking permission — run tests, install packages, edit files outside the task, make network calls, commit, push? Answer per tool, citing official docs.

3. Which coding agents run commands in a sandbox by default? Which ask for approval on each command? Which run with full user permissions? This is a separate question from #2.

4. Find documented incidents or research papers where malicious content in a repository (a README, an issue, a dependency file) caused a coding agent to do something harmful. Primary sources only.

5. Is there official vendor documentation or a well-known engineering write-up recommending you commit before running an agent, or review diffs instead of the agent's summary?

6. Find a study or documented case showing a coding agent's summary of its own work was inaccurate — claiming more verification than it did.

7. Besides hallucinated package names, what security flaws are over-represented in AI-generated code (SQL injection, hardcoded secrets, missing authorization, weak crypto)? Find papers or vendor advisories.

8. Do GitHub, Google, and OpenAI indemnify users against copyright claims on AI-generated code? Does the indemnity cover free tiers or paid tiers only? Find the actual terms pages. (I already have Anthropic's — skip them.)

9. Is free-tier code used for training by default on GitHub Copilot Free, Google Antigravity, Cursor Hobby, and Codex Free? Is there an opt-out? (I already know Copilot's default changed on 2026-04-24.)

10. Is there any emerging standard — employer policy, professional guidance, or conference/open-source policy — on disclosing that code was AI-generated?

11. Find documented cases where AI-assisted code caused a real production outage or security incident. Specific incidents with post-mortems or news coverage.

12. Do these free tiers work from the Philippines without a VPN or foreign payment method: Google Antigravity CLI, GitHub Copilot Free, Gemini API free tier, Groq? Are any region-restricted? (I already have the Philippine Data Privacy Act covered — skip it.)
